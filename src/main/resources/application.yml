spring:
  profiles:
    active:
      - local

    group:
      local:
        - db
        - oauth
        - h2
        - oauth-local
      dev:
        - db
        - oauth
        - oauth-dev
        - h2-in-memory
#        - mysql
  # 파일업로드 최대 용량 설정
  servlet:
    multipart:
      maxFileSize: 1MB
      maxRequestSize: 1MB

logging:
  file:
    name: /var/log/spring/spring.log

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

# 클라우드 설정
cloud:
  aws:
    s3:
      bucket: match-up-142023
    credentials:
      access-key: AKIAUQRV7XQ4YC2SIDNU
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false